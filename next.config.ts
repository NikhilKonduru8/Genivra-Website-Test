import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: {
    // buildActivity: false, // Removed as it is not a valid property
  },
};

export default nextConfig;
